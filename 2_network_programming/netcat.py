import argparse
import socket
import shlex
import subprocess
import sys
import textwrap
import threading

def tcp_client(host, port):
    

if __name__ == '__main__':
    # host = "www.google.com"
    # port = 80
    host = "127.0.0.1"
    port = 4444
    tcp_client(host, port)
